server:
    port: 8080
spring:
    application:
        name: backend
    profiles:
        active: @spring.profiles.active@
management:
    endpoints:
        web:
            exposure:
                include: "*"
logging:
    level:
        root: INFO
        web: INFO